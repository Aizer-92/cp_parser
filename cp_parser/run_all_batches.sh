#!/bin/bash
# Скрипт для последовательной обработки всех батчей

cd "$(dirname "$0")"

echo "========================================"
echo "🚀 МАССОВАЯ ОБРАБОТКА ВСЕХ БАТЧЕЙ"
echo "========================================"
echo ""

# Обрабатываем батчи с 3 по 14
for batch in {3..14}; do
    echo "----------------------------------------"
    echo "📦 БАТЧ #$batch"
    echo "----------------------------------------"
    
    python3 batch_fix_projects_full.py $batch
    
    if [ $? -ne 0 ]; then
        echo "❌ Ошибка в батче #$batch"
        exit 1
    fi
    
    echo ""
    echo "✅ Батч #$batch завершен"
    echo ""
    
    # Пауза между батчами
    sleep 3
done

echo "========================================"
echo "✅ ВСЕ БАТЧИ ОБРАБОТАНЫ!"
echo "========================================"

