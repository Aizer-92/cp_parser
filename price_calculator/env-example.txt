# =======================================================================
# 🔧 КОНФИГУРАЦИЯ PRICE CALCULATOR
# =======================================================================
# Скопируйте в .env и измените значения для production
# После рефакторинга ФАЗЫ 1: больше никакого хардкода в коде!

# =======================================================================
# 🔐 АУТЕНТИФИКАЦИЯ
# =======================================================================
# Логин и пароль для входа в систему
AUTH_USERNAME=admin
AUTH_PASSWORD=admin123

# Секретный ключ для сессий (ОБЯЗАТЕЛЬНО измените в production!)
# Генерируйте: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=your-super-secret-session-key-change-in-production

# =======================================================================
# 💱 КУРСЫ ВАЛЮТ
# =======================================================================
# Курс юаня к доллару (например, 1¥ = $0.139 при 7.2¥ за $1)
YUAN_TO_USD=0.139

# Курс доллара к рублю
USD_TO_RUB=84

# Примечание: YUAN_TO_RUB вычисляется автоматически (YUAN_TO_USD * USD_TO_RUB)

# =======================================================================
# 🗄️ БАЗА ДАННЫХ
# =======================================================================
# PostgreSQL (Railway/Production)
DATABASE_PUBLIC_URL=postgresql://username:password@host:port/database
DATABASE_PRIVATE_URL=postgresql://username:password@host:port/database

# SQLite (для локальной разработки)
# Если нет PostgreSQL, будет создан SQLite файл price_calculator.db

# =======================================================================
# 🚀 РАЗВЕРТЫВАНИЕ
# =======================================================================
# Порт приложения (Railway автоматически установит)
PORT=8000

# Режим разработки (true/false)
DEBUG=false

# Хост привязки
HOST=0.0.0.0

# =======================================================================
# 📊 ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ (для будущих фаз)
# =======================================================================
# Максимальное количество расчетов в истории
MAX_HISTORY_ITEMS=1000

# Таймаут сессии (в секундах)
SESSION_TIMEOUT=3600

# Логирование
LOG_LEVEL=INFO
LOG_FILE=/app/logs/price_calculator.log

# =======================================================================
# 🔮 БУДУЩИЕ РАСШИРЕНИЯ
# =======================================================================
# Интеграция с внешними API курсов валют
CURRENCY_API_KEY=your-api-key-here
CURRENCY_UPDATE_INTERVAL=3600

# Интеграция с ТН ВЭД сервисом
TNVED_API_URL=https://api.tnved.info/
TNVED_API_KEY=your-tnved-api-key

# Redis для сессий (в production вместо in-memory)
REDIS_URL=redis://localhost:6379/0

# Почтовые уведомления
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
ADMIN_EMAIL=admin@yourcompany.com

